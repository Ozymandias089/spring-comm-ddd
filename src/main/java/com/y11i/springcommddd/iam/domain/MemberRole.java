package com.y11i.springcommddd.iam.domain;

/**
 * 전역 사용자 역할
 *
 * <ul>
 *     <li>{@link #USER} 일반 사용자(기본)</li>
 *     <li>{@link #ADMIN} 전체 관리자</li>
 * </ul>
 */
public enum MemberRole {
    USER,
    ADMIN,
}
